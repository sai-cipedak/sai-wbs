import { supabaseClient } from './supabase-client.js';
import { mountIdentifiedEvidence } from './reporter-evidence.js?v=20260904-1';

const loginPanel=document.querySelector('#loginPanel');
const workspacePanel=document.querySelector('#workspacePanel');
const loginMessage=document.querySelector('#loginMessage');
const userLabel=document.querySelector('#userLabel');
const pageMessage=document.querySelector('#pageMessage');
const reportList=document.querySelector('#reportList');
const reportDetail=document.querySelector('#reportDetail');
let reports=[];
let selectedReportId=null;
function el(tag,text,cls){const n=document.createElement(tag);if(text!=null)n.textContent=text;if(cls)n.className=cls;return n;}
function fmt(v){return v?new Intl.DateTimeFormat('id-ID',{dateStyle:'medium',timeStyle:'short',timeZone:'Asia/Jakarta'}).format(new Date(v)):'—';}
function show(text,kind='info'){pageMessage.textContent=text;pageMessage.className=`form-message internal-message ${kind}`;pageMessage.hidden=!text;}
async function load(){show('');const{data,error}=await supabaseClient.functions.invoke('my-identified-reports',{body:{}});if(error){let msg=error.message;try{const c=await error.context?.json();if(c?.error)msg=c.error;}catch(_){}throw new Error(msg||'Laporan belum dapat dimuat.');}reports=data?.laporan??[];if(!reports.some((item)=>item.id===selectedReportId))selectedReportId=reports[0]?.id??null;renderList();renderSelected();}
function renderList(){reportList.replaceChildren();if(!reports.length){reportList.append(el('p','Belum ada laporan.','empty-state'));return;}for(const item of reports){const button=el('button',null,`case-list-button${item.id===selectedReportId?' active':''}`);button.type='button';button.setAttribute('aria-pressed',String(item.id===selectedReportId));button.append(el('strong',item.nomorLaporan),el('span',item.judul||'Laporan tanpa judul'),el('span',item.status,'status-badge'));button.addEventListener('click',()=>{selectedReportId=item.id;renderList();renderSelected();});reportList.append(button);}}
function renderSelected(){if(!reports.length){reportDetail.replaceChildren();const empty=el('section',null,'case-section');empty.append(el('h2','Belum ada laporan'),el('p','Laporan beridentitas yang Anda kirim akan muncul di sini.','muted'));const link=el('a','Sampaikan laporan','button primary');link.href='lapor-identitas.html';empty.append(link);reportDetail.append(empty);return;}const item=reports.find((row)=>row.id===selectedReportId)??reports[0];renderDetail(item);}
function renderDetail(item){reportDetail.replaceChildren();const head=el('section',null,'case-section');const top=el('div',null,'status-head');const title=el('div');title.append(el('p',item.nomorLaporan,'eyebrow'),el('h2',item.judul||'Laporan'),el('p',`Diterima ${fmt(item.tanggalLaporan)}`,'muted'));top.append(title,el('span',item.status,'status-badge'));head.append(top);const meta=el('div',null,'case-meta');[['Klasifikasi',item.klasifikasi||'Belum ditentukan'],['Status',item.status],['Nomor laporan',item.nomorLaporan],['Ditutup',item.closedAt?fmt(item.closedAt):'—']].forEach(([k,v])=>{const x=el('div');x.append(el('span',k),el('strong',v));meta.append(x);});head.append(meta);if(item.isTestData){const note=el('div',null,'notice');note.append(el('strong','Data UAT / Test'),el('p',item.testLabel||'Case ini ditandai sebagai data pengujian.'));head.append(note);}reportDetail.append(head);
 if(item.hasilAkhir){const final=el('section',null,'case-section');final.append(el('h3','Hasil Akhir'));for(const o of item.hasilAkhir.outcomes??[]){final.append(el('p',`Dugaan ${o.sequenceNo}: ${o.label}`,'case-copy'));}if(item.hasilAkhir.ringkasan)final.append(el('h4','Ringkasan'),el('p',item.hasilAkhir.ringkasan,'case-copy'));reportDetail.append(final);}
 const comm=el('section',null,'case-section');comm.append(el('h3','Komunikasi dengan Tim Penanganan'));if(!(item.pesan??[]).length)comm.append(el('p','Belum ada pesan.','muted'));for(const m of item.pesan??[]){const row=el('div',null,'message-item');row.append(el('strong',m.dari),el('p',m.isi),el('small',fmt(m.waktu)));comm.append(row);}if(item.canMessage){const ta=document.createElement('textarea');ta.rows=3;ta.placeholder='Tulis informasi tambahan atau balasan untuk Tim Penanganan.';const btn=el('button','Kirim pesan','secondary');btn.type='button';btn.addEventListener('click',async()=>{const text=ta.value.trim();if(text.length<5)return show('Pesan minimal 5 karakter.','error');try{btn.disabled=true;show('Mengirim pesan...');const{error}=await supabaseClient.functions.invoke('send-identified-message',{body:{caseId:item.id,message:text}});if(error){let msg=error.message;try{const c=await error.context?.json();if(c?.error)msg=c.error;}catch(_){}throw new Error(msg);}show('Pesan berhasil dikirim.');await load();}catch(e){show(e.message||'Pesan belum dapat dikirim.','error');btn.disabled=false;}});comm.append(ta,btn);}else comm.append(el('p','Laporan ini sudah ditutup. Kanal pesan baru tidak lagi dibuka.','muted'));reportDetail.append(comm);
 const evidenceHost=el('section',null,'case-section reporter-evidence-host');reportDetail.append(evidenceHost);mountIdentifiedEvidence(evidenceHost,item.id).catch((error)=>{const box=el('div',error.message||'Bukti belum dapat dimuat.','form-message error');evidenceHost.replaceChildren(box);});}
async function authorize(){const{data:{session}}=await supabaseClient.auth.getSession();if(!session?.user){loginPanel.hidden=false;workspacePanel.hidden=true;return;}loginPanel.hidden=true;workspacePanel.hidden=false;userLabel.textContent=session.user.email||'Akun Google';try{await load();}catch(e){show(e.message,'error');}}
document.querySelector('#googleLogin')?.addEventListener('click',async()=>{const redirectTo=new URL('my-reports.html',window.location.href).href;const{error}=await supabaseClient.auth.signInWithOAuth({provider:'google',options:{redirectTo}});if(error){loginMessage.textContent=error.message;loginMessage.className='form-message error';loginMessage.hidden=false;}});
document.querySelector('#refreshButton')?.addEventListener('click',load);
document.querySelector('#logoutButton')?.addEventListener('click',async()=>{await supabaseClient.auth.signOut();location.reload();});
await authorize();
